<% content_for(:title) {@page.title } %>
<% content_for(:description) {@page.description } if @page.description.present? %>
<% content_for(:keywords) {@page.keywords } if @page.description.present? %>

<%= @page.body.html_safe %>
