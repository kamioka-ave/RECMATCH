<div class="row">
  <div class="col-md-6">
    <%= simple_form_for([:admin, @cancel_reason_question]) do |f| %>
      <%= f.error_notification %>
      <%= f.input :name %>
      <%= f.input :rank %>
      <%= f.button :submit, class: 'btn-primary' %>
    <% end %>
  </div>
</div>
