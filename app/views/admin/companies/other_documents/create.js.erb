$('#other_documents').html("<%= escape_javascript(render('list', company: @company, documents: @company.other_documents)) %>");
$('#other_document_modal').modal('hide');
notify('', '<%= @document.name %>を登録しました', 'top', 'center', '', 'success', '', '');
