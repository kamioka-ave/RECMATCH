$('#preview_documents').html("<%= escape_javascript(render('list', company: @company, documents: @company.preview_documents)) %>");
$('#preview_document_modal').modal('hide');
notify('', '<%= @document.name %>を登録しました', 'top', 'center', '', 'success', '', '');
