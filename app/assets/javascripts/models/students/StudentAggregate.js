// @flow
export type StudentAggregate = {
  date: string,
  all_counts: number,
  new_counts: number,
  rejected_counts: number,
  in_review_counts: number,
  approved_counts: number,
  waiting_activation_counts: number,
  activated_counts: number,
};
